# 凯越邮箱 UI 重构验收矩阵

## 设计基线

- 品牌主色：`#1A3B70`，辅助色只用于焦点、状态与危险操作。
- 视觉方向：Apple Mail 的克制信息密度 + Linear 的精细边界与交互反馈。
- 统一系统字体、6–12px 圆角、1px 分隔线、清晰焦点环；不使用装饰性渐变。
- 动效限定在 `transform` 与 `opacity`，并支持 `prefers-reduced-motion`。
- 主流程文案、文件夹、日期时间与可访问名称以简体中文呈现。

## 窗口与表面覆盖

| 表面 | 覆盖内容 | 自动化证据 | 状态 |
| --- | --- | --- | --- |
| 登录与账户接入 | 凯越登录、服务商选择、基础账户、IMAP/SMTP、OAuth、成功页、首次偏好；标准与紧凑窗口 | `onboarding-visual.spec.ts`，8 张基线 | 通过 |
| 主邮箱工作区 | 品牌侧栏、标准/自定义文件夹、搜索、邮件列表、阅读器、附件、回复入口、紧凑布局 | `ui-surfaces-visual.spec.ts` | 通过 |
| 条件邮件界面 | 远程图片拦截、退订、账户认证错误、通知与恢复操作 | `ui-surfaces-visual.spec.ts` | 通过 |
| 空状态与草稿 | 空文件夹、草稿列表、草稿重新打开、发送状态 | 视觉基线 + 功能回归 | 通过 |
| 工作区布局 | 列表、左右分栏、上下分栏；浅色与深色主题组合 | `visual-snapshots.spec.ts`，6 张基线 | 通过 |
| 写信窗口 | 收件人/抄送/密送/回信地址、主题、富文本、附件、发送、稍后发送、模板 | `compose.spec.ts` + 视觉基线 | 通过 |
| 会话与预览窗口 | 会话独立窗口、快速预览、打印预览及其独立工具栏 | 功能回归 + 视觉基线 | 通过 |
| 移动与操作浮层 | 移动目录、稍后提醒、撤销提示、键盘关闭和回车操作 | 功能回归 + 视觉基线 | 通过 |
| 偏好设置 | 通用、账户、外观、快捷键、文件夹、规则、签名、模板、MCP 服务 | 数据持久化测试 + 视觉基线 | 通过 |
| 主题选择器 | 自动、浅色、深色、主题卡片与深色设置窗口 | 视觉基线 | 通过 |
| 联系人 | 联系人列表、阅读态、编辑态、搜索、中文姓名与字段类型、深色窗口 | 视觉基线 | 通过 |
| 日历 | 日、周、月、议程、事件详情、快速新建、搜索无结果、无日历、深色窗口 | 视觉基线 | 通过 |
| 搜索 | 普通关键词、`from:`、`subject:`、清除、结果加星和归档 | `search.spec.ts`，真实 FTS5 索引 | 通过 |
| 邮件与文件夹动作 | 展开/折叠、单选/多选、拖拽、归档、回收站、垃圾邮件、撤销、重命名和子文件夹 | 完整功能回归 | 通过 |
| 只读技术窗口 | 原始邮件、截断原文、账户日志的极简文档壳层 | 窗口行为回归 | 通过 |

## 确定性验收数据

`playwright/kaiyue-fixture.ts` 为每次测试生成隔离数据库，包含凯越 IMAP 账户、中文标准文件夹、自定义项目层级、业务邮件、草稿、PDF 附件、联系人、当天日历事件及真实 `ThreadSearch` 全文索引。测试不依赖外网、真实邮箱或历史数据库快照。

## 视觉基线统计

| 套件 | 数量 | 覆盖重点 |
| --- | ---: | --- |
| 登录接入 | 8 | 完整首次接入路径与紧凑窗口 |
| UI 表面 | 37 | 主工作区、条件状态、独立窗口、设置、联系人、日历与深色次级窗口 |
| 布局与主题 | 6 | 三种阅读布局 × 浅色/深色 |
| **合计** | **51** | **全部通过视觉回归** |

## 最终验证

```bash
npm run typecheck
npm run lint:check
npm run test:e2e -- --reporter=line
```

最终结果：类型检查通过、lint 通过、端到端测试 124 / 124 通过；视觉基线共 51 张。

视觉基线位于：

- `playwright/tests/onboarding-visual.spec.ts-snapshots/`
- `playwright/tests/ui-surfaces-visual.spec.ts-snapshots/`
- `playwright/tests/visual-snapshots.spec.ts-snapshots/`
